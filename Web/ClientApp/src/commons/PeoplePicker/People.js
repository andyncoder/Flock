
import React from 'react';

const People = (props) => {

    return(
        <div>
            <h3>{props.topic}</h3>
            <h4>{props.name}</h4>
        </div>
    )

}

export default People;